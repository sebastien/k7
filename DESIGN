

Naming convenions:

<modulename>.<modulename>.<CONSTANT>
<modulename>.<modulename>.<Class>
<modulename>.<modulename>.<Class>.<property>
<modulename>.<modulename>.<Class>.<someMethod>
<modulename>.<modulename>.<Class>.<ClassMethod>
<modulename>.<modulename>.<Class>.<ClassVariable>
<modulename>.<modulename>.<Class>.<CONSTANT>

module has a version, dependencies and init function

lib.async
	Reactor, Deferred, Future, Channels

system.posix
	all POSIX functions

system.thread
	create/join/destroy
	mutex/semaphore/

system.process

net.http

net.http.server

net.http.client

net.tcp

db.sqlite

db.tokyocabinet
